# Nockchain Jammer Configuration
# Copy this file to .env and customize as needed before running install.sh

# =============================================================================
# NOCKCHAIN PATHS (required for jam creation)
# =============================================================================
NOCKCHAIN_BIN=/home/nockchain/.cargo/bin/nockchain
NOCKCHAIN_DIR=/home/nockchain/nockchain

# =============================================================================
# INSTALLATION CONFIGURATION (used by install.sh)
# =============================================================================
# Repository to clone from
REPO_URL=https://github.com/nocktoshi/nockchain-jammer
# Local installation directory
INSTALL_DIR=/opt/nockchain-jammer
# Systemd service name
SERVICE_NAME=nockchain-jammer-api
# API server port
API_PORT=3001
# Directory for jam files and website
JAMS_DIR=/usr/share/nginx/html/jams
# Path to install the jam creation script
SCRIPT_PATH=/usr/local/bin/make-jam.sh
# Path to install the API binary
API_BINARY_PATH=/usr/local/bin/nockchain-jammer-api

# =============================================================================
# RUNTIME CONFIGURATION (used by the running API service)
# =============================================================================
# API key for authentication (leave blank to auto-generate)
API_KEY=
# Web root directory
HTML_ROOT=/usr/share/nginx/html
# Path to SHA256SUMS manifest file
MANIFEST=/usr/share/nginx/html/jams/SHA256SUMS
# Name of the nockchain systemd service
SERVICE_NAME_RUNTIME=nockchain
# Nockchain RPC endpoint
NOCKCHAIN_RPC=localhost:5556